# Solomon's Key (USA) iNES image layout.
#
# The upstream preservation listing uses PRG_BANK_0 for the complete 32 KiB
# CPU image and PRG_BANK_1 for the complete 32 KiB CHR image.  The latter name
# is historical; keeping it here allows the imported listing to remain byte
# stable while its semantic modularization proceeds.

MEMORY {
    HEADER: file = %O, start = $0000, size = $0010, fill = yes;
    PRG:    file = %O, start = $8000, size = $8000, fill = yes;
    CHR:    file = %O, start = $0000, size = $8000, fill = yes;
}

SEGMENTS {
    HEADER:     load = HEADER, type = ro;
    PRG_NMI:    load = PRG,    type = ro;
    PRG_NMI_GAMEPLAY_INTERACTIONS: load = PRG, type = ro;
    PRG_CONTROLLER_INPUT: load = PRG, type = ro;
    PRG_NMI_DANA_AND_SPRITES: load = PRG, type = ro;
    PRG_OBJECT_UPDATE: load = PRG, type = ro;
    PRG_OBJECT_MOTION: load = PRG, type = ro;
    PRG_OBJECT_COLLISION: load = PRG, type = ro;
    PRG_OBJECT_ANIMATION: load = PRG, type = ro;
    PRG_OBJECT_COLLISION_DISPATCH: load = PRG, type = ro;
    PRG_OBJECT_COLLISION_HANDLERS: load = PRG, type = ro;
    PRG_OBJECT_COLLISION_RESPONSE: load = PRG, type = ro;
    PRG_OBJECT_Y_THIRTEEN_CLAMP: load = PRG, type = ro;
    PRG_OBJECT_Y_CLAMP: load = PRG, type = ro;
    PRG_OBJECT_X_LEFT_CLAMP: load = PRG, type = ro;
    PRG_OBJECT_X_RIGHT_CLAMP: load = PRG, type = ro;
    PRG_OBJECT_MOTION_ANIMATION: load = PRG, type = ro;
    PRG_PPU_ATTRIBUTE_READ: load = PRG, type = ro;
    PRG_PPU_UPDATE_STREAM: load = PRG, type = ro;
    PRG_PRE_STARTUP_PADDING: load = PRG, type = ro;
    PRG_STARTUP: load = PRG, type = ro;
    PRG_SCHEDULER: load = PRG, type = ro;
    PRG_PAUSE_THREAD: load = PRG, type = ro;
    PRG_SOUND_EFFECT_QUEUE: load = PRG, type = ro;
    PRG_PPU_UPDATE_BUFFER: load = PRG, type = ro;
    PRG_JUMP_WITH_PARAMS: load = PRG, type = ro;
    PRG_ROOM_LOAD_PALETTE: load = PRG, type = ro;
    PRG_ROOM_CLEAR_THREAD: load = PRG, type = ro;
    PRG_ROOM_CLEAR_DATA: load = PRG, type = ro;
    PRG_ROOM_TIME_BONUS: load = PRG, type = ro;
    PRG_ROOM_LOAD_THREAD: load = PRG, type = ro;
    PRG_ROOM_LOAD_DATA: load = PRG, type = ro;
    PRG_NEW_GAME_STATE_RESET: load = PRG, type = ro;
    PRG_GAMEPLAY_DELAY_SETUP: load = PRG, type = ro;
    PRG_MASKED_RAM_WAIT: load = PRG, type = ro;
    PRG_COORDINATE_CONVERSION: load = PRG, type = ro;
    PRG_ROOM_DOOR_KEY_UPDATE: load = PRG, type = ro;
    PRG_ROOM_INTRO: load = PRG, type = ro;
    PRG_ROOM_INTRO_DATA: load = PRG, type = ro;
    PRG_TWO_DIGIT_NUMBER: load = PRG, type = ro;
    PRG_ROOM_ENTRY_ANIMATION: load = PRG, type = ro;
    PRG_ROOM_ENTRY_ANIMATION_DATA: load = PRG, type = ro;
    PRG_TRANSITION_OBJECT_ORBIT: load = PRG, type = ro;
    PRG_TRANSITION_ORBIT_POSITION: load = PRG, type = ro;
    PRG_TRANSITION_ORBIT_SCALE: load = PRG, type = ro;
    PRG_QUARTER_SINE: load = PRG, type = ro;
    PRG_QUARTER_SINE_DATA: load = PRG, type = ro;
    PRG_STATIC_PPU_UPDATE_QUEUE: load = PRG, type = ro;
    PRG_STATIC_PPU_UPDATE_POINTERS: load = PRG, type = ro;
    PRG_STATIC_PPU_UPDATE_STREAMS: load = PRG, type = ro;
    PRG_ROOM_ENEMY_LOAD: load = PRG, type = ro;
    PRG_ROOM_MAP_RENDER: load = PRG, type = ro;
    PRG_DIRECT_PPU_TRANSFER: load = PRG, type = ro;
    PRG_ROOM_NAMETABLE_FRAME: load = PRG, type = ro;
    PRG_PPU_DATA_WRITERS: load = PRG, type = ro;
    PRG_REPEATED_PPU_PATTERNS: load = PRG, type = ro;
    PRG_ROOM_ITEM_DECODE: load = PRG, type = ro;
    PRG_ROOM_ITEM_DECODE_DATA: load = PRG, type = ro;
    PRG_ROOM_BLOCK_DECODE: load = PRG, type = ro;
    PRG_DANA_ACTIONS: load = PRG, type = ro;
    PRG_COUNTER_WAIT: load = PRG, type = ro;
    PRG_MAP_INTERACTIONS: load = PRG, type = ro;
    PRG_COORDINATE_OBJECT_OVERLAP: load = PRG, type = ro;
    PRG_ROOM_MAP_CELL_UPDATE: load = PRG, type = ro;
    PRG_ROOM_MAP_PPU_ADDRESS: load = PRG, type = ro;
    PRG_MAIN_THREAD_PADDING: load = PRG, type = ro;
    PRG_MAIN_THREAD: load = PRG, type = ro;
    PRG_DEMON_MIRROR_ACTIVATION: load = PRG, type = ro;
    PRG_DEMON_MIRROR_SCHEDULE: load = PRG, type = ro;
    PRG_DEMON_MIRROR_SPAWN: load = PRG, type = ro;
    PRG_DEMON_MIRROR_INITIALIZATION: load = PRG, type = ro;
    PRG_TIMER: load = PRG, type = ro;
    PRG_TIMER_WARNING_DATA: load = PRG, type = ro;
    PRG_TIMER_DISPLAY: load = PRG, type = ro;
    PRG_ENEMY_MOVEMENT: load = PRG, type = ro;
    PRG_ENEMY_AI_DISPATCH: load = PRG, type = ro;
    PRG_FIREBALL_INVENTORY_DISPLAY: load = PRG, type = ro;
    PRG_FIREBALL_INVENTORY_DISPLAY_DATA: load = PRG, type = ro;
    PRG_FIREBALL_LIFETIME: load = PRG, type = ro;
    PRG_ENEMY_INITIALIZATION: load = PRG, type = ro;
    PRG_ENEMY_TYPE_CONFIGURATION: load = PRG, type = ro;
    PRG_ENEMY_TYPE_DATA: load = PRG, type = ro;
    PRG_ENEMY_AI_HANDLERS: load = PRG, type = ro;
    PRG_ENEMY_POSITION: load = PRG, type = ro;
    PRG_EARLY_ENEMY_AI: load = PRG, type = ro;
    PRG_MID_ENEMY_AI: load = PRG, type = ro;
    PRG_PATHFINDING_ENEMY_AI: load = PRG, type = ro;
    PRG_COLLISION_ENEMY_AI: load = PRG, type = ro;
    PRG_LATE_ENEMY_AI: load = PRG, type = ro;
    PRG_ENEMY_POINTERS: load = PRG, type = ro;
    PRG_LINKED_ENEMY_AI: load = PRG, type = ro;
    PRG_FIND_FREE_ENEMY_SLOT: load = PRG, type = ro;
    PRG_ENEMY_POINTER_TABLES: load = PRG, type = ro;
    PRG_ENEMY_DEACTIVATION: load = PRG, type = ro;
    PRG_CURRENT_ENEMY_DEACTIVATION: load = PRG, type = ro;
    PRG_ENEMY_LIFETIME: load = PRG, type = ro;
    PRG_FILLER_BEFORE_SPECIAL_ROOMS: load = PRG, type = ro;
    PRG_SPECIAL_ROOM_SCRIPTS: load = PRG, type = ro;
    PRG_ENDING_SEQUENCE: load = PRG, type = ro;
    PRG_ENDING_SPECIAL_ROOM_SUPPORT: load = PRG, type = ro;
    PRG_ENEMY_DROP_PROCESSING: load = PRG, type = ro;
    PRG_RANDOM_STATE: load = PRG, type = ro;
    PRG_KEY_COLLECTION_PRESENTATION: load = PRG, type = ro;
    PRG_KEY_DOOR_ANIMATION: load = PRG, type = ro;
    PRG_COORDINATE_DELTA: load = PRG, type = ro;
    PRG_ITEM_PRESENTATION: load = PRG, type = ro;
    PRG_GAMEPLAY_HUD: load = PRG, type = ro;
    PRG_GAMEPLAY_HUD_DATA: load = PRG, type = ro;
    PRG_SCORE_DISPLAY: load = PRG, type = ro;
    PRG_ITEM_COLLISION: load = PRG, type = ro;
    PRG_ITEM_HANDLER_TABLE: load = PRG, type = ro;
    PRG_RED_BOTTLE_ITEM: load = PRG, type = ro;
    PRG_SPECIAL_ITEM_FLAGS: load = PRG, type = ro;
    PRG_KEY_ITEM: load = PRG, type = ro;
    PRG_DOOR_ITEM: load = PRG, type = ro;
    PRG_GAMEPLAY_POOL_CLEAR: load = PRG, type = ro;
    PRG_TIMER_ITEM_EFFECTS: load = PRG, type = ro;
    PRG_INVENTORY_ITEM_EFFECTS: load = PRG, type = ro;
    PRG_ITEM_SCORE_TABLES: load = PRG, type = ro;
    PRG_AUXILIARY_EFFECT: load = PRG, type = ro;
    PRG_SCORE_ADDITION: load = PRG, type = ro;
    PRG_SECONDARY_THREAD_RESET: load = PRG, type = ro;
    PRG_GAMEPLAY_EXIT_FLOW: load = PRG, type = ro;
    PRG_GAMEPLAY_EXIT_DATA: load = PRG, type = ro;
    PRG_ROOM_TRANSITION_RESET: load = PRG, type = ro;
    PRG_NAMETABLE_CLEAR: load = PRG, type = ro;
    PRG_NAMETABLE_CLEAR_DATA: load = PRG, type = ro;
    PRG_SET_ACTIVE_OBJECT_STATES: load = PRG, type = ro;
    PRG_LOAD_OBJECT_POINTER: load = PRG, type = ro;
    PRG_DEACTIVATE_NON_DANA_OBJECTS: load = PRG, type = ro;
    PRG_ATTRACT_DEMO_FLOW: load = PRG, type = ro;
    PRG_FULL_NAMETABLE_CLEAR: load = PRG, type = ro;
    PRG_TITLE_SCREEN: load = PRG, type = ro;
    PRG_SET_PPU_ADDRESS: load = PRG, type = ro;
    PRG_TITLE_PACKED_DATA: load = PRG, type = ro;
    PRG_DEMO_INPUT_DATA: load = PRG, type = ro;
    PRG_FILLER_BEFORE_ROOM_TILE_PATTERNS: load = PRG, type = ro;
    PRG_ROOM_TILE_PATTERNS: load = PRG, type = ro;
    PRG_OBJECT_ANIMATION_DESCRIPTOR_POINTERS: load = PRG, type = ro;
    PRG_OBJECT_ANIMATION_DEFINITIONS: load = PRG, type = ro;
    PRG_OBJECT_ANIMATION_FRAMES: load = PRG, type = ro;
    PRG_OBJECT_MOTION_SELECTOR_POINTERS: load = PRG, type = ro;
    PRG_OBJECT_MOTION_SELECTORS: load = PRG, type = ro;
    PRG_OBJECT_MOTION_VALUES: load = PRG, type = ro;
    PRG_FILLER_BEFORE_DEMON_MIRROR_DATA: load = PRG, type = ro;
    PRG_DEMON_MIRROR_SCHEDULE_POINTERS: load = PRG, type = ro;
    PRG_DEMON_MIRROR_ENEMY_SET_POINTERS: load = PRG, type = ro;
    PRG_DEMON_MIRROR_SCHEDULE_DATA: load = PRG, type = ro;
    PRG_DEMON_MIRROR_ENEMY_SET_DATA: load = PRG, type = ro;
    PRG_ROOM_ENEMY_POINTERS: load = PRG, type = ro;
    PRG_ROOM_ENEMY_DATA: load = PRG, type = ro;
    PRG_ROOM_BLOCK_DATA: load = PRG, type = ro;
    PRG_ROOM_ITEM_POINTERS: load = PRG, type = ro;
    PRG_ROOM_ITEM_DATA: load = PRG, type = ro;
    PRG_FILLER_BEFORE_AUDIO: load = PRG, type = ro;
    PRG_AUDIO_ENGINE: load = PRG, type = ro;
    PRG_AUDIO_TIMING_TABLES: load = PRG, type = ro;
    PRG_AUDIO_ENVELOPES: load = PRG, type = ro;
    PRG_SOUND_EFFECT_DATA: load = PRG, type = ro;
    PRG_AUDIO_STREAMS: load = PRG, type = ro;
    VECTORS: load = PRG, type = ro;
    PRG_BANK_1: load = CHR,    type = ro;
}
