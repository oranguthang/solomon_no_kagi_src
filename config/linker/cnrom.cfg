# Solomon's Key (USA) iNES image layout.
#
# The upstream preservation listing uses PRG_BANK_0 for the complete 32 KiB
# CPU image and PRG_BANK_1 for the complete 32 KiB CHR image.  The latter name
# is historical; keeping it here allows the imported listing to remain byte
# stable while its semantic modularization proceeds.

MEMORY {
    HEADER: file = %O, start = $0000, size = $0010, fill = yes;
    PRG:    file = %O, start = $8000, size = $8000, fill = yes;
    CHR:    file = %O, start = $0000, size = $8000, fill = yes;
}

SEGMENTS {
    HEADER:     load = HEADER, type = ro;
    PRG_NMI:    load = PRG,    type = ro;
    PRG_PRE_STARTUP: load = PRG, type = ro;
    PRG_CONTROLLER_INPUT: load = PRG, type = ro;
    PRG_POST_CONTROLLER_INPUT: load = PRG, type = ro;
    PRG_OBJECT_Y_CLAMP: load = PRG, type = ro;
    PRG_OBJECT_X_LEFT_CLAMP: load = PRG, type = ro;
    PRG_POST_OBJECT_CLAMPS: load = PRG, type = ro;
    PRG_PPU_UPDATE_STREAM: load = PRG, type = ro;
    PRG_POST_PPU_UPDATE_STREAM: load = PRG, type = ro;
    PRG_STARTUP: load = PRG, type = ro;
    PRG_SCHEDULER: load = PRG, type = ro;
    PRG_PAUSE_THREAD: load = PRG, type = ro;
    PRG_SOUND_EFFECT_QUEUE: load = PRG, type = ro;
    PRG_PPU_UPDATE_BUFFER: load = PRG, type = ro;
    PRG_JUMP_WITH_PARAMS: load = PRG, type = ro;
    PRG_POST_JUMP_WITH_PARAMS: load = PRG, type = ro;
    PRG_GAMEPLAY_DELAY_SETUP: load = PRG, type = ro;
    PRG_MASKED_RAM_WAIT: load = PRG, type = ro;
    PRG_COORDINATE_CONVERSION: load = PRG, type = ro;
    PRG_POST_COORDINATE_CONVERSION: load = PRG, type = ro;
    PRG_STATIC_PPU_UPDATE_QUEUE: load = PRG, type = ro;
    PRG_STATIC_PPU_UPDATE_POINTERS: load = PRG, type = ro;
    PRG_STATIC_PPU_UPDATE_STREAMS: load = PRG, type = ro;
    PRG_POST_STATIC_PPU_UPDATE_DATA: load = PRG, type = ro;
    PRG_DIRECT_PPU_TRANSFER: load = PRG, type = ro;
    PRG_ROOM_NAMETABLE_FRAME: load = PRG, type = ro;
    PRG_PPU_DATA_WRITERS: load = PRG, type = ro;
    PRG_REPEATED_PPU_PATTERNS: load = PRG, type = ro;
    PRG_ROOM_ITEM_DECODE: load = PRG, type = ro;
    PRG_ROOM_ITEM_DECODE_DATA: load = PRG, type = ro;
    PRG_ROOM_BLOCK_DECODE: load = PRG, type = ro;
    PRG_POST_ROOM_BLOCK_DECODE: load = PRG, type = ro;
    PRG_COUNTER_WAIT: load = PRG, type = ro;
    PRG_POST_COUNTER_WAIT: load = PRG, type = ro;
    PRG_MAIN_THREAD: load = PRG, type = ro;
    PRG_PRE_TIMER: load = PRG, type = ro;
    PRG_TIMER: load = PRG, type = ro;
    PRG_PRE_TIMER_DISPLAY: load = PRG, type = ro;
    PRG_TIMER_DISPLAY: load = PRG, type = ro;
    PRG_ENEMY_MOVEMENT: load = PRG, type = ro;
    PRG_ENEMY_AI_DISPATCH: load = PRG, type = ro;
    PRG_PRE_FIREBALL_LIFETIME: load = PRG, type = ro;
    PRG_FIREBALL_LIFETIME: load = PRG, type = ro;
    PRG_ENEMY_INITIALIZATION: load = PRG, type = ro;
    PRG_ENEMY_TYPE_CONFIGURATION: load = PRG, type = ro;
    PRG_ENEMY_TYPE_DATA: load = PRG, type = ro;
    PRG_ENEMY_AI_HANDLERS: load = PRG, type = ro;
    PRG_ENEMY_POSITION: load = PRG, type = ro;
    PRG_PRE_ENEMY_POINTERS: load = PRG, type = ro;
    PRG_ENEMY_POINTERS: load = PRG, type = ro;
    PRG_PRE_ENEMY_POINTER_TABLES: load = PRG, type = ro;
    PRG_FIND_FREE_ENEMY_SLOT: load = PRG, type = ro;
    PRG_ENEMY_POINTER_TABLES: load = PRG, type = ro;
    PRG_ENEMY_DEACTIVATION: load = PRG, type = ro;
    PRG_CURRENT_ENEMY_DEACTIVATION: load = PRG, type = ro;
    PRG_BANK_0: load = PRG,    type = ro;
    PRG_TIMER_ITEM_EFFECTS: load = PRG, type = ro;
    PRG_INVENTORY_ITEM_EFFECTS: load = PRG, type = ro;
    PRG_ITEM_SCORE_TABLES: load = PRG, type = ro;
    PRG_AUXILIARY_EFFECT: load = PRG, type = ro;
    PRG_SCORE_ADDITION: load = PRG, type = ro;
    PRG_SECONDARY_THREAD_RESET: load = PRG, type = ro;
    PRG_POST_SECONDARY_THREAD_RESET: load = PRG, type = ro;
    PRG_ROOM_TRANSITION_RESET: load = PRG, type = ro;
    PRG_NAMETABLE_CLEAR: load = PRG, type = ro;
    PRG_NAMETABLE_CLEAR_DATA: load = PRG, type = ro;
    PRG_SET_ACTIVE_OBJECT_STATES: load = PRG, type = ro;
    PRG_LOAD_OBJECT_POINTER: load = PRG, type = ro;
    PRG_DEACTIVATE_NON_DANA_OBJECTS: load = PRG, type = ro;
    PRG_POST_NON_DANA_OBJECT_DEACTIVATION: load = PRG, type = ro;
    PRG_FULL_NAMETABLE_CLEAR: load = PRG, type = ro;
    PRG_POST_FULL_NAMETABLE_CLEAR: load = PRG, type = ro;
    PRG_SET_PPU_ADDRESS: load = PRG, type = ro;
    PRG_POST_SET_PPU_ADDRESS: load = PRG, type = ro;
    PRG_BANK_1: load = CHR,    type = ro;
}
